{
  "name": "@ruahlabs/tui-cli",
  "version": "0.1.0",
  "files": [
    "dist",
    "README.md"
  ],
  "bin": {
    "tui": "dist/bin.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts src/bin.ts --format esm --dts --external esbuild --external chokidar",
    "dev": "tsup src/index.ts src/bin.ts --format esm --dts --watch --external esbuild --external chokidar"
  },
  "dependencies": {
    "@ruahlabs/tui-core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.2"
  },
  "type": "module"
}