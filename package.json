{
  "name": "tui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -C examples/example-app dev",
    "release": "pnpm build && pnpm -r publish --access public"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@types/react": "^19.2.14",
    "chokidar": "^5.0.0",
    "esbuild": "^0.28.0",
    "ink": "^7.0.0",
    "react": "^19.2.5"
  }
}
